type Message {
  text: String
  createdAt: String
  createdBy: String
  id: ID
}

input MessageInput {
  text: String!
  username: String!
}

type Query {
  messages(id: ID!): Message
  me: String
}

type Mutation {
  createMessage(messageInput: MessageInput): Message
}
