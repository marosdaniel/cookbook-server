type Message {
  _id: ID
  text: String
  createdAt: String
  createdBy: String
}

input MessageInput {
  text: String!
  username: String!
}

type Query {
  messages(id: ID!): Message
  me: String
}

type Mutation {
  createMessage(messageInput: MessageInput): Message!
}
