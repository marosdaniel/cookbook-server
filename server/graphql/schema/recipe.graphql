type Recipe {
  _id: ID!
  title: String!
  description: String!
  createdAt: String!
  updatedAt: String!
  createdBy: String!
  authorId: ID! # ID of the user who created the recipe
}

input RecipeCreateInput {
  title: String!
  description: String!
  createdBy: String!
}

input RecipeEditInput {
  title: String
  description: String
}

type Query {
  getRecipeById(_id: ID!): Recipe!
  getRecipes(limit: Int): [Recipe]
  getRecipesByTitle(title: String!, limit: Int): [Recipe]
}

type Mutation {
  createRecipe(recipeCreateInput: RecipeCreateInput): Recipe!
  deleteRecipe(id: ID!): Boolean! # true if  something was deleted, false if the id was not found or deleted
  deleteAllRecipes: Int! # number of deleted recipes
  editRecipe(id: ID!, recipeEditInput: RecipeEditInput): Recipe!
}
